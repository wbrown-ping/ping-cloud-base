kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: elastic-stack-logging

resources:
- fluentbit.yaml
- configs

patchesStrategicMerge:
  # For customer-hub: uncommented by generate-cluster-state.sh to output only to S3 (no customer pipeline output).
  # For all other CDEs: remains commented → FluentBit outputs to both S3 and customer pipeline.
#- disable-logstash-chub-fluentbit-output-patch.yaml
